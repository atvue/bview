import { camlizeName } from '../../../utils/assist'
import { bviewPrefix as b } from '../../../utils/macro'

export const optionName = camlizeName( `${b}-option` ) ,
    selectName = camlizeName( `${b}-select` )